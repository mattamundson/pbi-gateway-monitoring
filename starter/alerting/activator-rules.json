{
  "_comment": "SOURCE OF TRUTH for gateway monitor alert rules. activator-rules.md is GENERATED from this file -- edit here, never there. Every rule is validated in CI by starter/tests/validate_alert_rules.py against starter/schemas/table_contracts.json: source_table must exist and be materialized, every condition identifier and message placeholder must be a real column, string literals must be inside the column's declared vocabulary, and percent-scaled columns may not be compared to 0-1 fractions.",
  "_source": "https://learn.microsoft.com/en-us/fabric/real-time-intelligence/data-activator/activator-introduction",
  "_guardrails": "Circuit breaker: if the same remediation fires >3x in 30 min for the same gateway, suppress + escalate to human. Destructive actions require Tier-2 human-in-loop approval (Teams Adaptive Card).",
  "_routing_note": "Route P1 alerts to a Teams CHANNEL, not to individual recipients. Activator throttles at 30 messages/recipient/hour but 500/item/hour -- during a fleet-wide outage, per-recipient routing silently drops messages exactly when they matter most.",
  "_history": "2026-07-21: validator (roadmap T14) found 4 of 7 rules could never fire and 2 more rendered broken messages. All fixed in this revision; see per-rule _fixed notes.",
  "rules": [
    {
      "name": "gateway-offline",
      "tier": 1,
      "pain": "#1 real-time offline alerting",
      "source_object": "GatewayHeartbeat (gold_gateway_health.heartbeat_age_minutes)",
      "source_table": "gold_gateway_health",
      "condition": "heartbeat_age_minutes > 10",
      "grouping": "per GatewayObjectId",
      "transition": "fires on state change to offline (Activator 'Becomes true', NOT 'Is true' -- 'Is true' re-fires every evaluation cycle and causes an alert storm during an extended outage)",
      "background": "No existing tool -- FPM, the PBIT template, or any third party -- provides proactive gateway-offline alerting without a Logic App or custom Power Automate flow. This is pain point #1's most-requested feature, implemented on native Fabric Activator with no Logic App.",
      "evidence": "\"No clue why, and no notification either\" -- https://community.fabric.microsoft.com/t5/Service/Gateway-status-emails-or-alerts/m-p/344928",
      "remediation": "Check the gateway host is powered on and the PBIEgwService Windows service is running. If the host is up but the service is down, see the runbook's gateway-offline decision tree. Datasets bound to this node will fail scheduled refresh until it returns.",
      "action": {
        "type": "notify",
        "channels": ["Teams", "email"],
        "message": "Gateway {GatewayObjectId} ({GatewayNodeName}, cluster {GatewayClusterName}) offline: no heartbeat for {heartbeat_age_minutes}m. Last seen {last_heartbeat_utc}."
      },
      "autonomous": true,
      "_fixed": "Threshold was 3 minutes against a 5-minute collector interval -- it would fire on every normal collection gap. Reconciled to 10 to match config.sample.json and activator-rules.md (roadmap T4). Message referenced {blast_radius}, which is not a column anywhere and would have rendered literally in every alert; replaced with real columns."
    },
    {
      "name": "spool-disk-forecast-low",
      "tier": 1,
      "pain": "#9 disk spooler surprises (proactive)",
      "source_object": "series_decompose_forecast output (see kql/02_anomaly_forecast.kql)",
      "source_table": "gold_predictions",
      "condition": "min_forecast_free_gb < 10",
      "grouping": "per GatewayHostName",
      "transition": "fires when 2h forecast crosses floor (proactive, before exhaustion)",
      "action": {
        "type": "notify",
        "channels": ["Teams"],
        "message": "Gateway {GatewayHostName} spool disk forecast to hit {min_forecast_free_gb}GB within {forecast_horizon_minutes}m."
      },
      "autonomous": true,
      "enabled": false,
      "blocked_by": "roadmap T42 -- gold_predictions is not materialized. 02_anomaly_forecast.kql is a reference query that no scheduled job runs, so nothing writes min_forecast_free_gb. Re-enable when the forecast job lands.",
      "_fixed": "Was enabled while pointing at a table nothing writes -- it appeared live on the board and could never fire. Now honestly disabled with the unblocking condition recorded."
    },
    {
      "name": "spool-disk-critical-remediate",
      "tier": 2,
      "pain": "#9 disk spooler surprises (remediation)",
      "source_object": "bronze_disk_spool",
      "source_table": "bronze_disk_spool",
      "condition": "FreeSpacePct < 5",
      "grouping": "per GatewayHostName",
      "background": "The gateway writes compressed query results to a spool directory. When that disk fills, queries fail with cryptic errors that never mention disk. No existing tool monitors it proactively. See https://learn.microsoft.com/en-us/data-integration/gateway/service-gateway-performance",
      "remediation": "Delete spool files older than the retention window, or expand the volume. If bronze_disk_spool.StreamBeforeRequestCompletes_Warning is true, spool size reporting is unreliable (SpoolingTotalDataSize reads 0 while streaming is active) -- trust FreeSpacePct over SpoolDirSizeBytes in that case.",
      "action": {
        "type": "user_data_function",
        "udf": "clear_spool_or_restart",
        "params": {
          "gatewayHost": "{GatewayHostName}",
          "action": "clear_spool"
        },
        "approval": "Teams Adaptive Card (Approve/Deny), 15-min timeout -> defer + ticket",
        "message": "Gateway {GatewayHostName} spool volume {SpoolDriveLetter} at {FreeSpacePct}% free ({SpoolDirSizeBytes} bytes spooled). Approve spool clear?"
      },
      "autonomous": false,
      "circuit_breaker": "max 3 fires / 30 min / host",
      "_note": "The only rule that was already correct. Its UDF action cannot execute until the on-prem action bridge exists (roadmap T45) -- Fabric User Data Functions do not support on-premises gateways."
    },
    {
      "name": "credential-drift",
      "tier": 1,
      "pain": "#10 credential state drift",
      "source_object": "datasource status (Get-GatewayInventory -CheckDatasourceStatus)",
      "source_table": "bronze_gateway_datasources",
      "condition": "Status != 'Live'",
      "grouping": "per DatasourceId",
      "transition": "requires 3 consecutive intervals in a non-Live state (debounce -- a transient probe failure is not credential drift). At the 15-minute inventory interval this is a deliberately slow ~45-minute alert.",
      "background": "Gateway datasource credentials go invalid between refresh runs (OAuth token expiry, service-account lockout, UNC path changes). The REST datasource status endpoint GET /gateways/{id}/datasources/{id}/status exists, but no tool uses it proactively for alerting.",
      "remediation": "Re-enter the datasource credentials: Power BI Service -> Settings -> Manage connections and gateways -> select the gateway -> Manage datasources -> select the datasource -> Edit credentials. Any dataset using this datasource will fail refresh until resolved.",
      "action": {
        "type": "notify",
        "channels": ["Teams"],
        "message": "Datasource {DatasourceName} ({DatasourceType}) on cluster {GatewayClusterName} status={Status}. Likely credential/OAuth expiry."
      },
      "autonomous": true,
      "_fixed": "Was 'datasource_status != Online' -- a column that exists nowhere, compared to a value outside the vocabulary (the collector emits Live/Unknown/Error, never Online), with three message placeholders that were also not columns of this table. Pain point #10 had never been capable of firing. Now keyed on the real column and vocabulary."
    },
    {
      "name": "error-rate-spike",
      "tier": 1,
      "pain": "#2 unified failure triage",
      "source_object": "gold_gateway_health (5-minute rolling window)",
      "source_table": "gold_gateway_health",
      "condition": "error_rate_pct > 25 AND query_count_5min > 10",
      "grouping": "per GatewayObjectId",
      "background": "The query_count_5min > 10 leg is deliberate: without a volume floor, a single failure in a 2-query window reads as a 50% error rate and pages someone at 3am over nothing.",
      "remediation": "Open the report's Failure Triage page for this gateway. Common causes in order of frequency: datasource connectivity, credential expiry (cross-check the credential-drift rule), network timeout (cross-check network-saturation).",
      "action": {
        "type": "notify",
        "channels": ["Teams"],
        "message": "Gateway {GatewayObjectId} error rate {error_rate_pct}% over {query_count_5min} queries in 5m (avg {duration_avg_ms}ms, p95 {duration_p95_ms}ms)."
      },
      "autonomous": true,
      "_fixed": "Was 'failure_rate_5m > 0.25 AND query_count_5m > 10' against bronze_query_execution. Neither column existed on any table; the correct columns are error_rate_pct and query_count_5min on gold_gateway_health. error_rate_pct is PERCENT-scaled, so the intended 25% threshold is 25, not 0.25 -- as originally written it would have fired on any window with a single error. The validator's scale check now blocks that specific confusion.",
      "_deferred": "The original action was run_notebook llm_alert_enrichment (LLM root-cause enrichment). Downgraded to notify: that notebook does not exist. Restore when the Data Agent explanation layer lands (roadmap T44)."
    },
    {
      "name": "cpu-anomaly",
      "tier": 1,
      "pain": "#5 mashup CPU/memory bloat",
      "source_object": "series_decompose_anomalies output (kql/02_anomaly_forecast.kql)",
      "source_table": "gold_predictions",
      "condition": "anomaly_score > 2.5",
      "grouping": "per GatewayObjectId",
      "action": {
        "type": "notify",
        "channels": ["Teams"],
        "message": "Gateway {GatewayObjectId} {metric} anomaly (score {anomaly_score}) vs seasonal baseline."
      },
      "autonomous": true,
      "enabled": false,
      "blocked_by": "roadmap T42 -- gold_predictions is not materialized. series_decompose_anomalies is a reference query that no scheduled job runs, so nothing writes anomaly_score. Re-enable when the anomaly job lands.",
      "_fixed": "Was enabled against a table nothing writes."
    },
    {
      "name": "mashup-container-runaway",
      "tier": 1,
      "pain": "#5 mashup memory/CPU bloat, per-process visibility",
      "source_object": "gold_mashup_health",
      "source_table": "gold_mashup_health",
      "condition": "peak_working_set_mb > 6000",
      "threshold_note": "6000 MB. Tune per host RAM -- see roadmap T25 (Day-0 calibration sets this from the host's own p99 rather than a shared default).",
      "grouping": "per GatewayObjectId / HostName",
      "transition": "fires when a mashup container's working set crosses the runaway threshold",
      "action": {
        "type": "notify",
        "channels": ["Teams"],
        "message": "Runaway mashup container on {HostName} ({GatewayObjectId}): peak {peak_working_set_mb}MB across {distinct_processes} processes, peak CPU {peak_cpu_pct}%."
      },
      "autonomous": true,
      "circuit_breaker": "max 3 fires / 30 min / host",
      "_fixed": "Condition carried an inline '// MB; tune per host RAM' comment. No expression surface accepts inline comments, so the condition was unparseable as written; the prose also tokenized into phantom column references. Moved to a threshold_note field."
    },
    {
      "name": "schema-drift",
      "tier": 1,
      "pain": "#4 gateway upgrades silently change log columns",
      "source_object": "bronze_schema_warnings (written by 01_bronze_ingest.py)",
      "source_table": "bronze_schema_warnings",
      "condition": "severity != 'added'",
      "grouping": "per log_type",
      "transition": "fires on any new row -- rows are written ONLY when drift is detected, so every row is actionable by construction",
      "background": "A gateway version upgrade can rename or drop a column in the CSV logs. The schema-adaptive parser absorbs the change without failing, which is correct for availability but means the change is otherwise INVISIBLE -- downstream silver transforms and DAX measures reference columns by name and start returning nulls with no error. This is pain point #4, and until roadmap T7 the signal was print()ed to notebook stdout and discarded.",
      "remediation": "Compare missing_columns against the current gateway version's documented schema (https://learn.microsoft.com/en-us/data-integration/gateway/service-gateway-performance). If a column was renamed, add the mapping to the parser's column contract. If dropped, audit which silver transforms and DAX measures reference it -- they are now silently producing nulls.",
      "action": {
        "type": "notify",
        "channels": ["Teams"],
        "message": "Gateway log schema drift in {log_type} (severity {severity}): missing [{missing_columns}], added [{added_columns}]. Expected {expected_column_count} columns, saw {actual_column_count}. Downstream transforms referencing the missing columns are now returning nulls."
      },
      "autonomous": true,
      "_note": "Condition excludes severity='added' because a purely additive change is forward-compatible -- mergeSchema absorbs it and nothing downstream breaks. Additive drift is still recorded in the table for audit, just not paged on."
    },
    {
      "name": "network-saturation",
      "tier": 2,
      "pain": "#7 network saturation blind spot (differentiator #4)",
      "source_object": "gold_gateway_health (network metrics from Collect-NetworkMetrics.ps1)",
      "source_table": "gold_gateway_health",
      "condition": "network_utilization_pct_avg > 80",
      "grouping": "per GatewayObjectId",
      "transition": "requires 3 consecutive intervals above threshold (sustained saturation, not a single burst)",
      "background": "The gateway-to-cloud NIC is a documented Microsoft blind spot -- no first-party tool surfaces it. Sustained saturation during large refreshes manifests to operators as slow or timing-out queries with no visible cause.",
      "remediation": "Schedule large refreshes off-peak, or add bandwidth. Correlate with gold_query_performance.spool_total_bytes_sum for the same window to confirm a refresh is the driver.",
      "action": {
        "type": "notify",
        "channels": ["email", "Teams"],
        "message": "Gateway {GatewayObjectId} ({GatewayNodeName}) NIC utilization {network_utilization_pct_avg}% sustained. Latency p95 {latency_ms_p95}ms, avg {latency_ms_avg}ms. Large refreshes may be saturating the gateway-to-cloud link."
      },
      "autonomous": true,
      "_fixed": "This rule existed ONLY in activator-rules.md and was absent from the JSON entirely -- part of the 5-vs-7 rule-count discrepancy (roadmap T4). Ported in and validated: network_utilization_pct_avg, latency_ms_p95 and latency_ms_avg are all real gold_gateway_health columns."
    }
  ]
}
